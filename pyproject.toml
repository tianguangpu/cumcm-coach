[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cumcm-coach"
version = "7.12.0"
description = "国赛(CUMCM)智能论文生成系统 — 题型识别 + 金标准内核 + 多求解器路由 + L1-L4 四级评审"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "tianguangpu" }]
keywords = [
    "cumcm",
    "mathematical-modeling",
    "latex",
    "typst",
    "optimization",
    "sensitivity-analysis",
    "claude-skill",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Text Processing :: Markup :: LaTeX",
]

# 核心依赖：任何使用场景都需要
dependencies = [
    "numpy>=1.24.0",
    "scipy>=1.10.0",
    "pandas>=2.0.0",
    "matplotlib>=3.7.0",
    "seaborn>=0.12.0",
]

[project.optional-dependencies]
# 求解器：LP/MIP/CSP 路由时使用
solvers = [
    "highspy>=1.7.0",
    "pulp>=2.7.0",
    "ortools>=9.6.2534",
]
# 全局灵敏度分析(Sobol / Morris / FAST)
sensitivity = ["SALib>=1.4.0"]
# 出版级图表样式(Nature / Science / IEEE)
viz = ["scienceplots>=2.1.0"]
# 文献检索与 Typst 参考文献解析
docs = [
    "requests>=2.28.0",
    "pyyaml>=6.0",
]
# 全部可选功能
full = [
    "highspy>=1.7.0",
    "pulp>=2.7.0",
    "ortools>=9.6.2534",
    "SALib>=1.4.0",
    "scienceplots>=2.1.0",
    "requests>=2.28.0",
    "pyyaml>=6.0",
]
# 开发依赖
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
    "ruff>=0.1.0",
    "black>=23.0.0",
    "mypy>=1.5.0",
    "pre-commit>=3.4.0",
]
# 时序预测完整版(TAM 需单独安装)
prediction = ["tam>=0.1.0"]

[project.urls]
Homepage = "https://github.com/tianguangpu/cumcm-coach"
Repository = "https://github.com/tianguangpu/cumcm-coach"
"Bug Tracker" = "https://github.com/tianguangpu/cumcm-coach/issues"
Changelog = "https://github.com/tianguangpu/cumcm-coach/blob/main/CHANGELOG.md"

[tool.setuptools.packages.find]
include = ["algorithms*", "utils*"]

[tool.setuptools.package-data]
algorithms = ["*.md"]

# ---------------- 工具配置 ----------------

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-ra --strict-markers"
markers = [
    "slow: 耗时较长的端到端测试",
    "smoke: 冒烟测试",
]

[tool.ruff]
line-length = 100
target-version = "py39"
# integrations/ 收录的是集成模块（含第三方内容），保持其原始风格
# 以便与上游同步，不纳入本项目的风格检查
extend-exclude = ["integrations"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM"]
ignore = [
    "E501",    # 行长由 formatter 控制
    "B008",    # 函数默认参数中调用函数（argparse 等常见用法）
    "SIM108",  # 三元表达式非强制
    # ---- 以下为项目既定风格，经逐项评估后保留 ----
    # 每项都写明理由，避免后来者误以为是遗漏
    "E741",    # 歧义变量名：SIR/SEIR 模型中 I（感染者）是数学标准符号，
               # 改为 infected 会破坏与流行病学文献的符号一致性
    "E701",    # 复合语句单行：约束分支 `if sense == "<=": A_ub.append(row)`
               # 的紧凑写法在数学建模代码中可读性更好
    "E702",    # 同上（分号形式）
    "SIM102",  # 嵌套 if：排版检查中「文档类型判断」与「具体规则判断」
               # 分两层比合并成长条件更易读
    "SIM115",  # 文件句柄未用 with：部分场景句柄需跨函数生命周期持有
    "B028",    # warnings.warn 缺 stacklevel：模块内警告，调用栈非关注点
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]
"scripts/*" = ["E402"]

[tool.black]
line-length = 100
target-version = ["py39", "py310", "py311", "py312", "py313"]

[tool.mypy]
python_version = "3.10"
warn_unused_configs = true
ignore_missing_imports = true
check_untyped_defs = false
